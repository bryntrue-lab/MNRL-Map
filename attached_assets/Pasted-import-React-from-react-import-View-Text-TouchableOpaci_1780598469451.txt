import React from 'react';
import { View, Text, TouchableOpacity, ScrollView, StyleSheet, Dimensions } from 'react-native';
import { ArchaicAtmosphere } from './Atmosphere';

const { width } = Dimensions.get('window');

// The 9 signal types — first 6 visible as chips, rest in "MORE" row
const CHIPS = [
  { id: 'dream',         label: 'Dream',         glyph: '◐' },
  { id: 'spark',         label: 'Spark',         glyph: '✦' },
  { id: 'resistance',    label: 'Resistance',    glyph: '◬' },
  { id: 'symbol',        label: 'Symbol',        glyph: '◯' },
  { id: 'synchronicity', label: 'Synchronicity', glyph: '❋' },
  { id: 'vision',        label: 'Vision',        glyph: '⌖' },
];

const MORE_CHIPS = ['Desire', 'Fear', 'Other'];

// Placeholder for recent captures — pulls from Firebase later
const RECENT_NOTES = [
  // Empty for now — empty state will render the prompt
];

export default function NotesScreen({ navigation }) {
  return (
    <View style={styles.container}>
      <ArchaicAtmosphere />

      <ScrollView
        contentContainerStyle={styles.scrollContent}
        showsVerticalScrollIndicator={false}
      >
        {/* Top bar */}
        <View style={styles.topBar}>
          <TouchableOpacity><Text style={styles.iconText}>≡</Text></TouchableOpacity>
          <Text style={styles.eyebrow}>NOTES</Text>
          <TouchableOpacity><Text style={styles.iconText}>⌕</Text></TouchableOpacity>
        </View>

        {/* Header */}
        <View style={styles.header}>
          <Text style={styles.title}>What's stirring?</Text>
          <Text style={styles.subtitle}>tap to capture a signal</Text>
        </View>

        {/* Capture chip grid */}
        <View style={styles.chipGrid}>
          {CHIPS.map(chip => (
            <TouchableOpacity
              key={chip.id}
              style={styles.chip}
              onPress={() => navigation.navigate('Capture', { type: chip.id })}
              activeOpacity={0.7}
            >
              <Text style={styles.glyph}>{chip.glyph}</Text>
              <Text style={styles.chipLabel}>{chip.label}</Text>
            </TouchableOpacity>
          ))}
        </View>

        {/* "More" row — less common signal types as text-only */}
        <Text style={styles.moreLabel}>
          MORE · {MORE_CHIPS.join(' · ').toUpperCase()}
        </Text>

        {/* Recent feed */}
        <View style={styles.recentSection}>
          <View style={styles.recentHeader}>
            <Text style={styles.recentLabel}>RECENT</Text>
            <Text style={styles.recentCount}>
              {RECENT_NOTES.length === 0 ? '' : `${RECENT_NOTES.length} in your field`}
            </Text>
          </View>

          {RECENT_NOTES.length === 0 ? (
            <View style={styles.emptyState}>
              <Text style={styles.emptyText}>
                your field is empty.{'\n'}
                tap a chip above to capture what's moving.
              </Text>
            </View>
          ) : (
            RECENT_NOTES.map((note, i) => (
              <View key={i} style={styles.noteItem}>
                {/* Note rendering — wire to real data */}
              </View>
            ))
          )}
        </View>
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#050208',
  },
  scrollContent: {
    paddingHorizontal: 28,
    paddingTop: 60,
    paddingBottom: 120,
  },

  // Top bar
  topBar: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 28,
  },
  iconText: {
    fontSize: 18,
    color: 'rgba(255, 255, 255, 0.6)',
  },
  eyebrow: {
    fontFamily: 'System',
    fontSize: 9,
    fontWeight: '500',
    letterSpacing: 2.5,
    color: 'rgba(255, 255, 255, 0.4)',
  },

  // Header
  header: {
    marginBottom: 28,
  },
  title: {
    fontFamily: 'System',
    fontSize: 26,
    fontWeight: '500',
    letterSpacing: -0.3,
    color: 'rgba(255, 255, 255, 0.95)',
    marginBottom: 6,
  },
  subtitle: {
    fontFamily: 'Iowan Old Style',
    fontStyle: 'italic',
    fontSize: 13,
    color: 'rgba(255, 255, 255, 0.5)',
  },

  // Chip grid
  chipGrid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: 8,
    marginBottom: 14,
  },
  chip: {
    width: (width - 56 - 16) / 3,   // (screen - paddings - gaps) / 3
    paddingVertical: 16,
    paddingHorizontal: 4,
    backgroundColor: 'rgba(255, 255, 255, 0.03)',
    borderWidth: 0.5,
    borderColor: 'rgba(255, 255, 255, 0.08)',
    borderRadius: 11,
    alignItems: 'center',
  },
  glyph: {
    fontSize: 18,
    color: 'rgba(255, 255, 255, 0.75)',
    marginBottom: 8,
    lineHeight: 22,
  },
  chipLabel: {
    fontFamily: 'System',
    fontSize: 11,
    color: 'rgba(255, 255, 255, 0.85)',
    letterSpacing: 0.2,
  },

  // More row
  moreLabel: {
    fontFamily: 'System',
    fontSize: 9,
    fontWeight: '500',
    letterSpacing: 2,
    color: 'rgba(255, 255, 255, 0.35)',
    textAlign: 'center',
    paddingVertical: 14,
    marginBottom: 24,
  },

  // Recent section
  recentSection: {
    marginTop: 12,
  },
  recentHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 14,
  },
  recentLabel: {
    fontFamily: 'System',
    fontSize: 9,
    fontWeight: '500',
    letterSpacing: 2.5,
    color: 'rgba(255, 255, 255, 0.4)',
  },
  recentCount: {
    fontFamily: 'Iowan Old Style',
    fontStyle: 'italic',
    fontSize: 11,
    color: 'rgba(255, 255, 255, 0.4)',
  },

  // Empty state
  emptyState: {
    paddingVertical: 32,
    alignItems: 'center',
  },
  emptyText: {
    fontFamily: 'Iowan Old Style',
    fontStyle: 'italic',
    fontSize: 13,
    lineHeight: 22,
    color: 'rgba(255, 255, 255, 0.4)',
    textAlign: 'center',
  },

  // Note item (placeholder structure)
  noteItem: {
    paddingVertical: 12,
    borderBottomWidth: 0.5,
    borderBottomColor: 'rgba(255, 255, 255, 0.06)',
  },
});