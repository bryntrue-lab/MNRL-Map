import React, { useState } from 'react';
import { View, Text, TextInput, TouchableOpacity, StyleSheet, Dimensions } from 'react-native';
import Svg, { Defs, RadialGradient, Stop, Rect } from 'react-native-svg';

const { width, height } = Dimensions.get('window');

function ArchaicAtmosphere() {
  return (
    <Svg width={width * 2} height={height * 0.75} style={styles.atmosphere} pointerEvents="none">
      <Defs>
        <RadialGradient id="archaicGlow" cx="50%" cy="20%" rx="60%" ry="55%" fx="50%" fy="20%">
          <Stop offset="0%"   stopColor="#3D1E3D" stopOpacity="0.85" />
          <Stop offset="35%"  stopColor="#2A1530" stopOpacity="0.6" />
          <Stop offset="65%"  stopColor="#1A0D1F" stopOpacity="0.25" />
          <Stop offset="100%" stopColor="#050208" stopOpacity="0" />
        </RadialGradient>
      </Defs>
      <Rect x="0" y="0" width="100%" height="100%" fill="url(#archaicGlow)" />
    </Svg>
  );
}

export default function SignatureScreen({ navigation }) {
  const [birthDate, setBirthDate] = useState('');
  const [birthTime, setBirthTime] = useState('');
  const [birthLocation, setBirthLocation] = useState('');

  return (
    <View style={styles.container}>
      <ArchaicAtmosphere />

      {/* Content cluster — vertically centered */}
      <View style={styles.contentWrap}>

        {/* Header */}
        <Text style={styles.eyebrow}>YOUR SIGNATURE</Text>
        <Text style={styles.title}>When were you born?</Text>
        <Text style={styles.subtitle}>This anchors your spiral life map.</Text>

        {/* Unlocks — quieter, no enclosing box */}
        <View style={styles.unlocks}>
          <Text style={styles.unlocksLabel}>UNLOCKS</Text>
          <Text style={styles.unlockItem}>your 28-year cycle</Text>
          <Text style={styles.unlockItem}>your bodygraph + type</Text>
          <Text style={styles.unlockItem}>readings keyed to your design</Text>
        </View>

        {/* Form fields — thinner, lighter, more elegant */}
        <View style={styles.fields}>
          <TextInput
            style={styles.input}
            placeholder="birth date"
            placeholderTextColor="rgba(255,255,255,0.3)"
            value={birthDate}
            onChangeText={setBirthDate}
          />
          <TextInput
            style={styles.input}
            placeholder="birth time  (optional)"
            placeholderTextColor="rgba(255,255,255,0.3)"
            value={birthTime}
            onChangeText={setBirthTime}
          />
          <TextInput
            style={styles.input}
            placeholder="birth location"
            placeholderTextColor="rgba(255,255,255,0.3)"
            value={birthLocation}
            onChangeText={setBirthLocation}
          />
        </View>
      </View>

      {/* Footer — progress dots + continue, with skip as a quieter secondary action */}
      <View style={styles.footer}>
        <View style={styles.dots}>
          <View style={styles.dot} />
          <View style={styles.dot} />
          <View style={[styles.dot, styles.dotActive]} />
          <View style={styles.dot} />
          <View style={styles.dot} />
        </View>
        <TouchableOpacity onPress={() => navigation.navigate('Practice')}>
          <Text style={styles.continueText}>continue →</Text>
        </TouchableOpacity>
      </View>

      {/* Skip — quieter, above the footer, centered, much smaller */}
      <TouchableOpacity
        onPress={() => navigation.navigate('Practice')}
        style={styles.skipWrap}
      >
        <Text style={styles.skipText}>skip · add later</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#050208',
  },
  atmosphere: {
    position: 'absolute',
    top: 0,
    left: -width * 0.5,
  },

  // Content wrap fills available vertical space and centers content
  contentWrap: {
    flex: 1,
    paddingHorizontal: 36,
    paddingTop: height * 0.10,
    paddingBottom: 140,
    justifyContent: 'center',
  },

  // Header
  eyebrow: {
    fontFamily: 'System',
    fontSize: 10,
    fontWeight: '500',
    letterSpacing: 2.5,
    color: 'rgba(255, 255, 255, 0.45)',
    marginBottom: 12,
  },
  title: {
    fontFamily: 'System',
    fontSize: 26,
    fontWeight: '500',
    letterSpacing: -0.3,
    color: 'rgba(255, 255, 255, 0.95)',
    marginBottom: 10,
  },
  subtitle: {
    fontFamily: 'Iowan Old Style',
    fontStyle: 'italic',
    fontSize: 15,
    lineHeight: 22,
    color: 'rgba(255, 255, 255, 0.6)',
    marginBottom: 32,
  },

  // Unlocks — minimal, no border or box
  unlocks: {
    marginBottom: 32,
  },
  unlocksLabel: {
    fontFamily: 'System',
    fontSize: 9,
    fontWeight: '500',
    letterSpacing: 2.5,
    color: 'rgba(168,156,220,0.7)',     // lavender — integral, signals the depth this unlocks
    marginBottom: 10,
  },
  unlockItem: {
    fontFamily: 'System',
    fontSize: 13,
    lineHeight: 22,
    color: 'rgba(255, 255, 255, 0.7)',
  },

  // Form fields — thinner, more elegant
  fields: {
    gap: 10,
  },
  input: {
    paddingVertical: 13,
    paddingHorizontal: 16,
    backgroundColor: 'rgba(255,255,255,0.03)',
    borderWidth: 0.5,
    borderColor: 'rgba(255,255,255,0.1)',
    borderRadius: 10,
    fontSize: 14,
    color: 'rgba(255, 255, 255, 0.95)',
    fontFamily: 'System',
  },

  // Footer
  footer: {
    position: 'absolute',
    bottom: 56,
    left: 36,
    right: 36,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  dots: {
    flexDirection: 'row',
    gap: 7,
    alignItems: 'center',
  },
  dot: {
    width: 5,
    height: 5,
    borderRadius: 2.5,
    backgroundColor: 'rgba(255, 255, 255, 0.22)',
  },
  dotActive: {
    width: 22,
    height: 5,
    borderRadius: 2.5,
    backgroundColor: 'rgba(255, 255, 255, 1)',
  },
  continueText: {
    fontFamily: 'System',
    fontSize: 14,
    fontWeight: '400',
    color: 'rgba(255, 255, 255, 0.88)',
    letterSpacing: 0.4,
  },

  // Skip — sits above footer, centered, MUCH quieter
  skipWrap: {
    position: 'absolute',
    bottom: 100,
    left: 0,
    right: 0,
    alignItems: 'center',
  },
  skipText: {
    fontFamily: 'System',
    fontSize: 11,
    letterSpacing: 1.5,
    color: 'rgba(255, 255, 255, 0.35)',
  },
});