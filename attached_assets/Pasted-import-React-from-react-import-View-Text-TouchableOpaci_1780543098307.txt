import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet, ScrollView, Dimensions } from 'react-native';
import Svg, { Polygon } from 'react-native-svg';
import { ArchaicAtmosphere } from './Atmosphere';
import { SpiralIndicator } from './SpiralComponents';

const { width, height } = Dimensions.get('window');

// Small play triangle for the encounter Begin button
function PlayIcon() {
  return (
    <Svg width={10} height={11} viewBox="0 0 10 11">
      <Polygon points="0,0 0,11 10,5.5" fill="#050208" />
    </Svg>
  );
}

export default function TodayScreen({ navigation }) {
  // Hardcoded for now — wire to Firebase later
  const currentEncounter = {
    phase: 'signal',
    turn: 'first',
    title: 'The Threshold',
    subtitle: "something is calling — what comes when you stop naming it?",
    durationMin: 3,
  };

  return (
    <View style={styles.container}>
      <ArchaicAtmosphere />

      <ScrollView
        contentContainerStyle={styles.scrollContent}
        showsVerticalScrollIndicator={false}
      >
        {/* Top bar — menu icon + settings icon, no title */}
        <View style={styles.topBar}>
          <TouchableOpacity><Text style={styles.iconText}>≡</Text></TouchableOpacity>
          <Text style={styles.eyebrow}>TODAY</Text>
          <TouchableOpacity><Text style={styles.iconText}>⊙</Text></TouchableOpacity>
        </View>

        {/* Spiral indicator — the visual anchor of Today */}
        <View style={styles.spiralWrap}>
          <SpiralIndicator phase={currentEncounter.phase} turn={currentEncounter.turn} />
        </View>

        {/* Current encounter card — the actual day's content */}
        <View style={styles.encounterCard}>
          <Text style={styles.encounterEyebrow}>TODAY'S ENCOUNTER</Text>
          <Text style={styles.encounterTitle}>{currentEncounter.title}</Text>
          <Text style={styles.encounterSubtitle}>{currentEncounter.subtitle}</Text>

          <TouchableOpacity
            style={styles.beginButton}
            onPress={() => navigation.navigate('Encounter', { encounter: currentEncounter })}
            activeOpacity={0.85}
          >
            <View style={styles.playCircle}>
              <PlayIcon />
            </View>
            <View style={styles.beginTextWrap}>
              <Text style={styles.beginLabel}>Begin</Text>
              <Text style={styles.beginMeta}>{currentEncounter.durationMin} MIN · VOICE</Text>
            </View>
          </TouchableOpacity>
        </View>

        {/* Past encounters affordance — quiet, below the fold */}
        <TouchableOpacity
          style={styles.pastWrap}
          onPress={() => navigation.navigate('PastEncounters')}
        >
          <Text style={styles.pastText}>what you've walked through →</Text>
        </TouchableOpacity>
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#050208',
  },
  scrollContent: {
    paddingHorizontal: 28,
    paddingTop: 60,         // safe-area top
    paddingBottom: 100,     // room above tab bar
  },

  // Top bar
  topBar: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 28,
  },
  iconText: {
    fontSize: 18,
    color: 'rgba(255, 255, 255, 0.6)',
  },
  eyebrow: {
    fontFamily: 'System',
    fontSize: 9,
    fontWeight: '500',
    letterSpacing: 2.5,
    color: 'rgba(255, 255, 255, 0.4)',
  },

  // Spiral indicator wrap
  spiralWrap: {
    alignItems: 'center',
    marginBottom: 36,
  },

  // Current encounter card — NOT a bordered box, just a content block
  encounterCard: {
    alignItems: 'center',
    paddingHorizontal: 8,
    marginBottom: 48,
  },
  encounterEyebrow: {
    fontFamily: 'System',
    fontSize: 9,
    fontWeight: '500',
    letterSpacing: 2.5,
    color: 'rgba(196, 74, 138, 0.85)',     // archaic pink — signals Phase I encounter
    textAlign: 'center',
    marginBottom: 12,
  },
  encounterTitle: {
    fontFamily: 'System',
    fontSize: 28,
    fontWeight: '500',
    letterSpacing: -0.3,
    color: 'rgba(255, 255, 255, 0.98)',
    textAlign: 'center',
    marginBottom: 12,
  },
  encounterSubtitle: {
    fontFamily: 'Iowan Old Style',
    fontStyle: 'italic',
    fontSize: 15,
    lineHeight: 23,
    letterSpacing: 0.1,
    color: 'rgba(255, 255, 255, 0.62)',
    textAlign: 'center',
    maxWidth: 280,
    marginBottom: 36,
  },

  // Begin button — same as Threshold/Begin screen
  beginButton: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 14,
    paddingVertical: 14,
    paddingHorizontal: 22,
    backgroundColor: 'rgba(255, 255, 255, 0.92)',
    borderRadius: 999,
    minWidth: 180,
    justifyContent: 'center',
  },
  playCircle: {
    width: 28,
    height: 28,
    borderRadius: 14,
    backgroundColor: 'rgba(196, 74, 138, 0.18)',
    alignItems: 'center',
    justifyContent: 'center',
    paddingLeft: 2,
  },
  beginTextWrap: {
    alignItems: 'flex-start',
  },
  beginLabel: {
    fontFamily: 'System',
    fontSize: 15,
    fontWeight: '500',
    color: '#050208',
    letterSpacing: 0.1,
    lineHeight: 18,
  },
  beginMeta: {
    fontFamily: 'System',
    fontSize: 9,
    fontWeight: '500',
    letterSpacing: 1.8,
    color: 'rgba(5, 2, 8, 0.55)',
    marginTop: 1,
  },

  // Past encounters affordance
  pastWrap: {
    alignItems: 'center',
    marginTop: 24,
    paddingVertical: 12,
  },
  pastText: {
    fontFamily: 'System',
    fontSize: 12,
    letterSpacing: 1.5,
    color: 'rgba(255, 255, 255, 0.4)',
  },
});